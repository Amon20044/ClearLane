# ClearLane — pipeline + backend (Python 3.11)
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
scipy>=1.12
lightgbm>=4.0
shap>=0.45
pyarrow>=15
fastapi>=0.110
uvicorn[standard]>=0.29
# optional — only for the LLM copilot deployment extension (CLEARLANE_LLM=1)
# anthropic>=0.40
